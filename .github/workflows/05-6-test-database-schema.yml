name: "05.6 - Modelo de base de datos"

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - name: Ejecutar prueba 5.6
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: python scripts/test_database_schema.py
